# Roadmap

Долгий горизонт: рубежи и постоянный техдолг. Работа, которая ведётся прямо
сейчас, живёт в `.scratch/<feature-slug>/` — строка отсюда переезжает туда,
когда её берут в работу, и отмечается здесь, когда приземляется.

Текущая работа: [`.scratch/buddy-redesign/`](../.scratch/buddy-redesign/spec.md)

---

## Что уже работает

Пайплайн приёма данных, который переживает редизайн без изменений в основе:

- [x] Nuxt 4 + Nitro, WebSocket endpoint `/ws`, broadcast-сервис
- [x] `POST /api/gsi` с валидацией и санитизацией payload
- [x] Защита shared secret (`NUXT_GSI_SECRET` / `auth.token`)
- [x] Idle при пустом или невалидном payload
- [x] `GET /api/health`, уровни логирования, структурированные логи
- [x] Runtime-конфигурация endpoint-ов

Клиентская часть переписывается целиком — см. спеку, §7.

---

## Рубеж 0 — проба эфира

Один вечер, почти без кода. Проверяем гипотезу ADR-0003 до того, как на ней
построен визуальный язык.

- [x] [01](../.scratch/buddy-redesign/issues/01-broadcast-alpha-probe.md) —
      тест-карта в Streamlabs, три подложки, ADR-0003 уточнён по замеру

Осталось от этого выезда, вернуть при следующем: как бадди выглядит поверх
Доты после даунскейла 2K → 1080p, и даёт ли `fpsLimit = 12` во сне реальные
12 кадров.

**Критерий:** запрет на мягкое свечение перестаёт быть гипотезой, выведенной
из чужих баг-репортов.

---

## Рубеж 1 — читается ли аффект

Проверка главной гипотезы до вложений в остальное.

- [x] [02](../.scratch/buddy-redesign/issues/02-client-shell.md) — каркас
      нового клиента
- [x] [03](../.scratch/buddy-redesign/issues/03-control-panel-affect.md) —
      пульт, уровень аффекта
- [x] [04](../.scratch/buddy-redesign/issues/04-buddy-eye-two-axes.md) —
      бадди с глазом на двух осях
- [x] Пять визуалов с переключением, контракт визуала, палитра по замеру
      фона игры
- [ ] [05](../.scratch/buddy-redesign/issues/05-affect-readability-check.md) —
      проверка читаемости на людях

Требования к рендеру из разведки OBS (один рендер, без `power-preference`,
null-обработка WebGL, анимации от `delta`, маленький backing store) — не
отдельные задачи, а критерии приёмки тикета 02: прототип сносится целиком,
чинить в нём нечего.

**Критерий:** состояние бадди читается человеком, не знающим Доту, без
подсказок. Если нет — переделывать форму здесь, а не после четырёх надстроек.

---

## Рубеж 2 — реальные данные

- [x] [07](../.scratch/buddy-redesign/issues/07-control-panel-snapshots.md) —
      пульт, уровень снапшотов: синтетические GSI-пакеты в `POST /api/gsi`
- [x] [08](../.scratch/buddy-redesign/issues/08-server-event-derivation.md) —
      серверный слой вывода событий из снапшотов (ADR-0002)
- [x] [09](../.scratch/buddy-redesign/issues/09-affect-signal-mapping.md) —
      отображение сигналов матча на оси аффекта
- [x] Снимок состояния при подключении клиента
- [x] Сброс серверного состояния на границе матча
- [x] [10](../.scratch/buddy-redesign/issues/10-session-record-replay.md) —
      запись GSI-сессии в файл
- [ ] [10](../.scratch/buddy-redesign/issues/10-session-record-replay.md) —
      воспроизведение записи в реальном темпе, управление с пульта
- [x] [11](../.scratch/buddy-redesign/issues/11-gsi-field-measurements.md) —
      **замер:** доходит ли блок `events` до обычного игрока — да, доходит,
      и несёт события всей игры
- [x] [11](../.scratch/buddy-redesign/issues/11-gsi-field-measurements.md) —
      **замер:** задержка «событие в игре → POST» — медиана 1 с игрового
      времени, максимум 2 с
- [ ] [11](../.scratch/buddy-redesign/issues/11-gsi-field-measurements.md) —
      **замер:** даёт ли `fpsLimit = 12` во сне реальные 12 кадров

**Критерий:** записанная бот-сессия проигрывается и даёт те же реакции, что
живой матч.

---

## Рубеж 3 — слои

- [ ] Потребитель блока `events`: события всей игры вторым источником
- [ ] Периферия по релевантности (здоровье, респавн, байбек, ультимейт)
- [ ] Справочная строка GPM/XPM с трендом
- [ ] [13](../.scratch/buddy-redesign/issues/13-escalation-tiers.md) — три
      уровня эскалации: объявлены в модели, визуально не выражены
- [ ] Вехи: аганим и шард (таланты вехой не считаются — см. `CONTEXT.md`)

Носитель вех не выбран. Орбиты из прототипа для этого не годятся: в
концепции с глазом они спорят с морганием и пересекают его. Обсуждалось, что
у аганима и шарда могут быть собственные элементы, а не общий счётчик
колец, — отдельная развилка, разбирается после выбора основной формы.
- [ ] Взгляд как канал внимания
- [ ] Позиционирование через query-параметр
- [ ] Состояние сна вне матча

Тикета на потребителя блока `events` пока нет — есть только замер. Выезд
2026-09-20 показал, что блок доходит и обычному игроку и несёт события всей
игры: убийства по всем десяти, башни, казармы, Рошан, аегис, байбеки, дым,
глиф, скан, первая кровь, тормент. Это **второй источник** рядом с диффингом
своего героя, а не его продолжение: другая механика (скользящее окно,
медиана 27 повторов на событие) и другой разбор (`generic_event` держит
`data` строкой с JSON внутри). Он же — единственный правдоподобный материал
для средних и полноэкранных уровней эскалации (тикет 13): своего героя на
2–3 пика за матч не хватит. Подробности — в
[`gsi-data-surface.md`](research/gsi-data-surface.md), §4.

**Критерий:** полный матч отсматривается без ощущения шума и без мёртвых
участков.

---

## Рубеж 4 — эфир

- [ ] Приёмка альфы и краёв **в OBS**, не в браузере (ADR-0003)
- [ ] Профилирование кадра: `obs-source-profiler`, бюджет против 16.6 мс
- [ ] Прогон 3+ часа, контроль VRAM (obs-studio#11549)
- [ ] Чеклист перед эфиром — написать заново под новый оверлей

**Критерий:** трёхчасовой стрим без деградации и без потери FPS в файтах.

---

## Отложено намеренно

Решения приняты, работа не ведётся. Причины — в спеке.

- **Звук.** Событийный слой уже нормализован, точка подключения появится
  бесплатно. Конкурирует с голосом стримера, делается последним.
- **Сводка сессии.** Требует персистентности. Эмоциональный тон исхода
  читается из последнего снапшота без хранилища.
- **Раздача другим стримерам.** Темы, пресеты, онбординг. Архитектура не
  мешает, но конфигурируемость — налог на каждую фичу.
- **Интерактив с чатом.** Отдельный продукт поверх этого.

---

## Переход на сетевую топологию

Сейчас тестирование идёт вариантом (a): репозиторий разворачивается на
игровом ПК, всё локально. Цель — приложение остаётся на машине разработки,
игровой ПК шлёт по локальной сети. Короткая петля «поправил здесь — увидел
там» без переноса кода.

- [ ] Сервер слушает не только loopback
- [ ] `NUXT_PUBLIC_WS_URL` на сетевой адрес
- [ ] `NUXT_GSI_SECRET` обязателен — трафик идёт по сети, а не внутри машины
- [ ] Описать обе топологии в README

---

## Постоянный техдолг

- [x] Клиент на TypeScript — закрыто вместе с каркасом (тикет 02)
- [ ] Стратегия логирования для production (`silent` или внешний sink)
- [ ] Статус WS-соединения в UI (connected / reconnecting / offline)
- [ ] Ручное гейтирование рендер-лупа в покое. Сейчас во сне стоит
      `fpsLimit` сцены, а не гейт; действительно ли он даёт заявленные
      кадры — не измерено (тикет 11, пункт 4)
