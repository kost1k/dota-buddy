# 15. Команда проверки типов должна проверять типы

Status: done
Type: task

## Зачем

`npm run typecheck` был `vue-tsc --noEmit`, а корневой `tsconfig.json` — это
чистый список `references` на `.nuxt/tsconfig.{app,server,shared,node}.json`
с `"files": []`. Без `--build` компилятор видит ноль входных файлов, молча
ничего не делает и возвращает 0.

Замерено:

| Команда | Код возврата | Ошибок |
| --- | --- | --- |
| `vue-tsc --noEmit` | 0 | 0, вывод пуст |
| `vue-tsc --build --force` | 2 | 3 |

То есть команда не просто не находила ошибок — она сообщала об успехе, и
ошибки копились. Нашлось это по касательной, при работе над тикетом 14.

## Что сделано

### Скрипт

`"typecheck": "nuxt typecheck"`.

Рассматривался и `vue-tsc --build`. Оба ловят одно и то же, и у обоих
проверено главное подозрение — не даёт ли прогретый кеш ложного «всё ок»:

- три прогона `vue-tsc --build` подряд без `--force` — каждый раз код 2 и те
  же три ошибки, диагностика повторяется из `.tsbuildinfo`, а не теряется;
- `.tsbuildinfo` лежит в `.nuxt/`, а он в `.gitignore`;
- при отсутствующем `.nuxt` `vue-tsc --build` падает громко (`TS5083`,
  код 1), а не проходит молча.

Решил `nuxt typecheck`: он пересобирает `.nuxt` перед проверкой, то есть
снимает единственный оставшийся способ соврать — **устаревшие**
сгенерированные типы. Разница в цене 0.4 с (2.15 против 2.55 с).

### Фантомный визуал

`shared/visual.ts` объявлял `VisualId` из шести членов, включая `'breath'`,
тогда как `VISUALS` и `REGISTRY` в `BuddyStage.vue` содержат пять. Оба
`Record<VisualId, …>` не удовлетворяли собственному типу.

Происхождение: коммит 6fd68c3 «drop the breath variant» удалил компонент
`VisualBreath.vue` и запись из `VISUALS`, но не тронул объединение. Удаление
было осознанным (обоснование там же, в сообщении коммита и в тикете 05),
поэтому правильное направление — убрать член объединения, а не возвращать
визуал.

Поведение в рантайме не меняется: `isVisualId` проверяет членство в
`VISUALS`, так что `?visual=breath` и раньше откатывался к `DEFAULT_VISUAL`.

## Приёмка

Скрипт проверен на то, что он и падает, и проходит:

| Что | Код возврата |
| --- | --- |
| Заведомая ошибка типа в `shared/visual.ts` | 2 |
| Заведомая ошибка типа в `app/components/BuddyStage.vue` | 2 |
| Чистое дерево, первый прогон | 0 |
| Чистое дерево, второй прогон подряд | 0 |

Обе намеренные ошибки затем убраны, дерево вернулось к двум задуманным
правкам.

`?visual=` проверен в браузере: `crystal` переключает на кристалл,
`breath` и `nope` откатываются к кольцу. `npx vitest run` — 228 тестов,
`npx eslint app/ shared/ test/` — чисто.

## Comments

### Про «кольцо не отрисовывается во сне» — отбой

Здесь ранее было записано, что кольцо во сне не рисуется, а кристалл в тех
же условиях виден. **Дефекта нет, это был артефакт измерения.**

**Причина — закрытая панель браузера.** При закрытой панели страница
получает `document.visibilityState === 'hidden'`, и Chromium душит
`requestAnimationFrame` до фонового режима. Замер чистым счётчиком в самой
странице, без участия приложения:

| Панель | Кадров за 2 с | `visibilityState` |
| --- | --- | --- |
| закрыта | 3 (1.5 к/с) | `hidden` |
| открыта | 241 (120 к/с) | `visible` |

Цикл TresJS построен на `rAF`, поэтому при закрытой панели вся сцена шла
~1.5 кадра в секунду независимо от `asleep` и от `fps-limit`, а скриншот
возвращал устаревший композит: один раз кольцо было на снимке, хотя
`onBeforeRender` того экземпляра страницы не записал ни строки.

Добавочно: страница тогда рисовалась примерно в одну шестьдесят четвёртую
кадра (1024×768 укладывались в ~125×95 точек кадра 800×600), то есть canvas
320 px превращался в ~40 px. Этого одного хватает, чтобы объяснить исходную
асимметрию — сплошной кристалл на таком размере оставляет несколько видимых
точек, полое тонкое кольцо не оставляет ни одной.

### Замер при открытой панели

Повторено начисто, счётчик кадров внутри `onBeforeRender`:

| Визуал | Режим | Кадров/с | На экране |
| --- | --- | --- | --- |
| Кольцо | сон | 11.2 | да |
| Кольцо | бодрствование | 120 | да |
| Кристалл | сон | 11.3 | да |

`theme.scene.sleepFps` равен 12 — ограничение отрабатывает точно, бадди во
сне остаётся на экране и продолжает двигаться на пониженной частоте, то
есть ровно как обещают комментарий в `BuddyStage.vue:70-73` и раздел «Сон» в
`CONTEXT.md`.

Вывод для метода: **о рендере судить только при открытой панели.** Записано
в `CLAUDE.md` к остальным измеренным ограничениям среды.
